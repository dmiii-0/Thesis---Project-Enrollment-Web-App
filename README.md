# FlashPoint

This is a Project Enrollment Web App that the students can use as a project library.

For Proof of Concept, the system is capable of deploying/injecting codes into microcontrollers (Arduino, ESP32, Raspberry Pi)
without the need of external IDEs.

The system repository is locally hosted via Gitea, requiring a few modifications to run properly.
